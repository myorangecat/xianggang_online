<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dodo.xianggang_online">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/icon_app2"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_icon_me"
        android:supportsRtl="true"
        android:theme="@style/Theme.Login_design"
        >
        <activity android:name=".about"></activity>
        <activity android:name=".setting" />
        <activity android:name=".start"
            android:theme="@style/Theme.MyApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".getStateInfoFile"
            android:enabled="true"
            android:exported="true" />
        <service
            android:name=".MyService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".MainActivity2"
            android:theme="@style/Theme.MyApp_2"
            ></activity>
        <activity android:name=".kongya_three" />
        <activity android:name=".kongya_tow" />
        <activity android:name=".one" />
        <activity android:name=".police" />
        <activity android:name=".search" />
        <activity android:name=".home" />
        <activity android:name=".MainActivity"
            ></activity>
    </application>

</manifest>